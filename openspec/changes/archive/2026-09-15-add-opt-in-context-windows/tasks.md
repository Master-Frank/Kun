## 1. 设置与协议

- [x] 1.1 扩展 Shared contextCompaction 类型、defaults、normalize、merge 和迁移，加入 windowModeEnabled=false；验证缺失/非法值为关闭，开关往返保存不改变摘要参数。
- [x] 1.2 扩展 Kun config schema 和 Main runtime config 双向映射/热更新；以配置集成测试验证 GUI 到 Runtime 的完整传递。
- [x] 1.3 定义版本化窗口 item/event、历史/笔记请求结果及 turn 模式快照契约；验证序列化、旧会话读取和三种 endpoint 兼容的工具 schema。

## 2. 本地持久化与生命周期

- [x] 2.1 为窗口索引和可查询历史增加 Manager 操作/port，保证 squash 前记录可恢复；验证原始消息查询和分页扫描不依赖 SSE 日志或常驻全量数组。
- [x] 2.2 实现窗口检查点的 revision 原子提交和 operation 幂等；验证并发 append、提交冲突、写入失败、取消及崩溃后重放不丢历史。
- [x] 2.3 实现线程私有笔记与版本序列、revision 替换和幂等追加；验证路径边界、写入冲突、重启恢复及 design.md 限额。
- [x] 2.4 接入 fork/resume/archive/delete 和旧会话按需初始化；验证历史分叉点笔记快照、父子隔离、归档保留和删除清理。

## 3. 工具与预算

- [x] 3.1 实现四个 history 工具和五个 notes 工具，使用可信执行身份；验证跨线程访问拒绝、搜索定位、分页、附件引用和请求/输出双向限额。
- [x] 3.2 将工具接入现有注册/发现/执行层，以接受 turn 的模式控制；验证关闭时无 schema 且直接调用被拒绝，非 GUI 客户端也可用。
- [x] 3.3 实现窗口预算计算和 25/50/75% 去重提示，接入完整请求估计与模型 profile；验证大输入、多阈值跨越、工具输出、模型切换和输出预留。
- [x] 3.4 实现有界初始恢复指针和工具使用说明；通过请求快照验证固定 system 前缀不变、无自动全文笔记注入、无旧窗口对话泄漏。

## 4. 窗口策略与循环接入

- [x] 4.1 在 turn 接受时冻结模式并传给续跑/子代理；验证热更新不影响活动 turn，子代理仅继承模式而不获得父线程数据访问权。
- [x] 4.2 实现 new_context 互斥批次预检查和换窗协调服务；验证混合批次零副作用、待审批/待输入阻止换窗、同 turn 后续请求和无进展重置限制。
- [x] 4.3 统一自动预检、send-boundary、内存压力和 provider overflow 策略分发；验证开启时摘要模型调用数为零，硬限不绕过，单次失败恢复及部分输出不重放。
- [x] 4.4 更新模型历史投影、history repair 和上下文初始化以识别窗口边界；验证有效工具配对、权威 goal/plan/skills 状态、steering 输入和重启恢复。
- [x] 4.5 保留手动 compact 摘要并登记窗口边界，支持禁用后的摘要策略；验证开关往返、混合摘要/窗口检查点和既有摘要测试无回归。

## 5. 设置与时间线

- [x] 5.1 在设置的实验室区域添加“窗口式上下文（实验性）”独立开关及说明；实际渲染验证默认关闭、保存重载、窄窗口布局和摘要配置仍保留。
- [x] 5.2 接入事件 mapper/projection 和窗口时间线标记；验证 SSE 重连去重、顺序、原始对话可见且无新任务或诊断面板。

## 6. 整体验证与文档

- [x] 6.1 增加完整场景测试：保存笔记、换窗、检索旧决定、继续任务、重启和历史分叉；同时验证无笔记时通过任务指针恢复、存储故障及并发新输入。
- [x] 6.2 对 chat completions、messages、responses 使用适配器测试验证工具声明/结果和上下文投影；核查不支持工具的路线在清窗前给出明确错误。
- [x] 6.3 对相同长任务比较开关两种状态，记录正确恢复、请求次数、摘要/检索 token 和 cache 使用；交付验证结果，避免把开关默认改为开启。
- [x] 6.4 更新 docs/kun-architecture.md 和用户设置说明，描述限制、手动摘要和关闭回退；验证文档与实际开关行为一致。
- [x] 6.5 运行 npm run typecheck、相关 vitest、npm run build:kun、npm run build、npm run check:file-lines 和 git diff --check；记录结果并区分既有失败与本次回归。
- [x] 6.6 在开发版桌面端使用真实 provider 验证预算、实际换窗、同 turn 续跑、Runtime 重启和旧窗口历史检索；将过程、发现、修复及最终门禁记录到 design.md。
